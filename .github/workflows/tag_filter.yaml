# 특정 태그(패턴)에서 발생
name: tag-filter
on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+' # v1.0.0 or v2.2.2 or v1.0 or 1.0.0

jobs:
  tag-filter:
    runs-on: ubuntu-latest
    steps:
      - name: echo hello
        run: echo hello